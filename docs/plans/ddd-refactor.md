# MedAppServer

Домашняя аптечка: что есть в наличии и что пора принять. Аптечки бывают общими — семья, соседи. Система приватна по замыслу: о человеке не хранится ничего, кроме идентификатора и хеша ключа.

Документ состоит из трёх частей: **что система должна уметь**, **как она устроена**, **что делаем**.

---
---

# Часть 1. Фичи и критерии

Перечень полный: и то, что уже работает и должно продолжать работать, и то, чего ещё нет. Отмечено: ✅ работает, ⬜ ещё нет.

## 1.1 Аутентификация и доступ

| | фича | критерий |
|---|---|---|
| ✅ | Регистрация `POST /v1/auth/register` | Выдаёт сгенерированные учётные данные. Закрыта секретом `X-Registration-Token`; в проде пустой секрет не даёт приложению стартовать. |
| ✅ | Токен `POST /v1/auth/token` | HTTP Basic → JWT, срок жизни в claim `exp`. |
| ✅ | Некорректный Basic | 401, а не 500 и не stack trace. |
| ✅ | Троттлинг выдачи токена | По адресу клиента, окно настраивается; превышение — 429. Каждая попытка стоит проверки bcrypt, поэтому без троттлинга анонимный вызывающий жжёт CPU бесплатно. |
| ✅ | Доверенный forwarded-адрес | Адрес из заголовка берётся только от доверенного прокси; иначе троттлинг обходится подделкой заголовка. |
| ✅ | Форма ключей | URL-safe, без padding: ключ ездит в URL приглашения. |
| ✅ | Область публичных эндпойнтов | Публичны ровно два маршрута аутентификации, остальное требует токена. В контракте это объявлено у каждой операции отдельно: умолчания на уровне документа нет, иначе «отвечает без токена» и «забыли написать» неразличимы. |

## 1.2 Приватность

| | фича | критерий |
|---|---|---|
| ✅ | Минимум данных о человеке | В `users` только `id` и `hashed_key`. Ни имени, ни почты, ни телефона. |
| ✅ | Тела ошибок ничего не раскрывают | Ответ несёт класс проблемы, но никогда — идентификаторы и количества. Проверяется отдельным тестом на утечку. |
| ✅ | Недоступное неотличимо от несуществующего | Чужая упаковка и несуществующая отвечают одинаково: иначе по коду ответа перебором узнаётся, что чужая запись есть. |
| ✅ | Приёмы не попадают в БД | Журнал приёмов слишком персонален для таблицы. Хранится вне БД; смена хранилища меняет репозиторий, а не сущность. |

## 1.3 Аптечки и участники

| | фича | критерий |
|---|---|---|
| ✅ | Создать, прочитать, перечислить | Список отдаёт счётчики участников и упаковок без загрузки содержимого; чтение одной — вместе с содержимым. |
| ✅ | Приглашение | Ключ с ограниченным сроком, принимается многократно в течение него; на сервере лежит только хеш ключа. |
| ✅ | Вступление и выход | Повторное вступление отвергается; выход уносит брони вышедшего внутри этой аптечки. |
| ✅ | Аптечка не бывает без участников | Выход последнего уносит аптечку вместе с содержимым. |
| ✅ | Удаление, при желании с переносом | Без переноса содержимое уходит каскадом; с переносом брони тех, кто целевую аптечку не видит, исчезают. |
| ✅ | Равноправие участников | Ни владельца, ни ролей: у аптечки нет хозяина. |
| ✅ | Гонки членства закрыты | Двое последних не могут выйти одновременно; вступление против удаления не оставляет участника внутри удалённой аптечки. |

## 1.4 Упаковки

**Ключевое понятие новой концепции: `Drug` — это упаковка, а не «лекарство вообще».**

| | фича | критерий |
|---|---|---|
| ✅ | Завести упаковку в аптечке | Аптечка задаётся путём: упаковка не существует сама по себе. |
| ✅ | Прочитать, править описание | Отсутствующее поле в PATCH означает «не трогать». |
| ✅ | Исправление количества | Только исправление учёта — в любую сторону. **Пополнения не бывает: докупил пачку — завёл вторую упаковку.** |
| ✅ | Списание | Съеденное уменьшает упаковку. **Списать больше, чем в упаковке, нельзя** — это физика, а не политика. Различия «приём по плану» и «расход вне плана» больше нет. |
| ✅ | Ноль уничтожает упаковку | Пустая пачка выбрасывается: списание до нуля удаляет запись вместе с бронями на неё. |
| ✅ | Перенос между аптечками | Брони тех, кто целевую аптечку не видит, не переживают переезда. |
| ✅ | Точные количества | `NUMERIC(19,6)`, округление вниз; половина таблетки не превращается в `0.49999999999999994`. |
| ✅ | Упаковка не знает о бронях | Ни суммы внутри агрегата, ни «доступного остатка», ни пропорционального пересчёта. |

## 1.5 Брони

**Второе ключевое понятие: бронь — заявка человека на часть упаковки, со своей жизнью.**

| | фича | критерий |
|---|---|---|
| ✅ | Завести, прочитать, изменить, отменить | Одна бронь на пару «человек и упаковка». Отмена — это удаление; брони с нулём не бывает. |
| ✅ | Бронь может превышать остаток | 30 таблеток в пачке, забронировано 40 — нормальное состояние. **Сервер не чинит планы клиента**: сколько из брони оставить, решает её владелец. |
| ✅ | Бронь не переживает упаковку | Бронь на выброшенную пачку бессмысленна и исчезает вместе с ней. |
| ✅ | Мои брони по всем упаковкам | Один запрос, не зависящий от числа упаковок. |
| ✅ | Своя версия у картины броней | Токен принадлежит снимку броней на упаковку, а не отдельной брони: изменить свою долю, не увидев общей суммы, нельзя — иначе решение принимается вслепую. Слито в #100. |

## 1.6 Синхронизация клиента

| | фича | критерий |
|---|---|---|
| ✅ | Атомарный запрос на упаковку (#100) | Съеденное и новая бронь приезжают вместе и применяются в одной транзакции. Разрыва «сначала списание, сработали уведомления, потом уменьшение брони» не бывает. |
| ✅ | Съеденное — дельта, бронь — абсолют (#100) | Дельта коммутативна и предусловия не требует; бронь клиент задаёт целиком, потому что она и есть его решение. |
| ✅ | Идемпотентность по клиентскому идентификатору (#100) | Повтор того же запроса не списывает второй раз; тот же идентификатор с другим содержимым — 409. В журнал попадает только **закоммиченное**. Граница названа: журнал живёт в памяти процесса — не переживает рестарт и не работает между экземплярами. |
| ✅ | Уничтожение упаковки внутри запроса (#100) | Если списание опустошило пачку, часть про бронь ничего не делает: бронь исчезла вместе с упаковкой. Это не ошибка. |

## 1.7 Справочник

| | фича | критерий |
|---|---|---|
| ✅ | Поиск | По названию, латинскому названию, действующему веществу и производителю; полнотекст плюс триграммы, чтобы находилось и с опечаткой. |
| ✅ | Ранжирование | Точное совпадение выше префикса, префикс выше подстроки, опечатка ниже всего. |
| ✅ | Границы выдачи | Лимит зажат в 1..50 в сервисе, а не только в контроллере: сервис вызывается не только из HTTP. |
| ✅ | Словари единиц и форм | Общие для справочника и заведённых руками упаковок: «шт» — одна единица на систему, а не две одинаково написанные строки. |
| ✅ | Импорт | Данные заливаются мимо приложения; приложение справочник только читает. |
| ✅ | Локаль и индексы | `simple`-конфигурация полнотекста (стемминг ломает торговые названия), GIN по документу и триграммам. |

## 1.8 Схема и данные

| | фича | критерий |
|---|---|---|
| ✅ | Один `db/schema.sql`, создание с нуля | Ни миграций, ни `ALTER`. `ddl-auto=validate` во всех профилях: расхождение схемы и сущностей роняет старт. |
| ✅ | Каскады описаны и проверены | Каскад со стороны аптечки и упаковки есть; со стороны пользователя нет — удаление человека не операция API, и молчаливо выносить чужие данные оно не должно. |
| ✅ | Постгрес и в тестах | Testcontainers, а не H2: поведение зависит от СУБД. |

## 1.9 Публичный контракт

| | фича | критерий |
|---|---|---|
| ✅ | Ресурсный `/v1` | Старые маршруты отдают 404. |
| ✅ | RFC 7807 на ошибках | Один формат, включая отказы уровня безопасности. |
| ✅ | Снапшот OpenAPI в репозитории | Расхождение с генерируемым контрактом роняет сборку. |
| ✅ | У каждой операции есть описание | Текст живёт в `@Operation` рядом с методом. Эндпойнт без описания ломает сборку, а не появляется в контракте безымянным. |
| ✅ | Снимок пользователя | Всё, что видно вызывающему, одним ответом — для синхронизации. |
| ✅ | Версии и предусловия | Версия едет **полем тела**, а где тела нет — параметром запроса; `ETag` и `If-Match` не используются: заголовок один, а ответ упаковки несёт два независимых состояния. Не прислано — 428, нечитаемо — 400, предъявлено и не совпало — **412**, недоступно — 404. 409 отвечает не за предусловие, а за конфликт при записи: дубль брони, версии из тела синхронизации. Слито в #100. Коды **объявлены в контракте** (#108): 428 и 412 проставляются по факту наличия версии, синхронизация — названное исключение с 409. |

## 1.10 Гейты качества

| | фича | критерий |
|---|---|---|
| ✅ | Контрактный тест маршрутов | Каждая операция живёт по заявленному пути, старых путей нет. |
| ✅ | Пользовательские сценарии | Сквозные истории: завести, поделиться, выйти, перенести, удалить. |
| ✅ | Тесты отката | Упавшая команда не оставляет частичных изменений. |
| ✅ | Гонки (#100) | Списание, членство, перенос против выхода, заведение брони против потери доступа, одновременное создание. Проигравшая сторона обязана получить отказ, а состояние — совпасть с результатом победившей: одного «итог верный» мало. |
| ✅ | Планы запросов (#101) | Отдельный source set, `EXPLAIN (FORMAT JSON)`, реестр из 31 метода хранилищ, отчёты артефактом CI. Найдено: нечёткий поиск читает справочник целиком — индекс применим, но на 18 000 записей планировщик его не выбирает. |
| ✅ | N+1-гейты (#107) | Число запросов не растёт с объёмом: снимок 4, содержимое аптечки 4, свои брони 1, удаление с переносом 9, без переноса 3, выход 5, переезд 10, уничтожение упаковки 6 — одинаково на обоих размерах. Считает `RecordedSql` из #101, а не Hibernate Statistics, которых нет с #96. Утверждается равенство между размерами, числа выкладываются отчётом. Найдено и починено: `recountSnapshots` шёл циклом — удаление аптечки со ста упаковками стоило 109 запросов вместо 9. |
| ✅ | Архитектурные тесты (#102) | Пятнадцать правил закреплены, три сняты явно: репозиториев и сущностей нет, `EntityManager` ушёл, «одно понятие — один тип» держит компилятор. Список — в `ARCHITECTURE-RULES.md`. |
| ✅ | Цепочка поставок (#109) | CodeQL по серверу на PR, push и расписанию; `dependency-review` роняет PR с уязвимой зависимостью; `dependabot.yml` на Gradle сервера и на сами actions; у каждого workflow объявлены минимальные `permissions`. Проверка контрольных сумм отложена — см. долги. Заодно скраппер вынесен в отдельный архивный репозиторий и подключён подмодулем. |

---
---

# Часть 2. Архитектура

## 2.1 Ограниченные контексты

**Аптечка** — основной. Упаковки, брони, аптечки, участники, пользователи.

**Справочник** — данные внешнего скраппера Vidal. Только чтение, наполняется мимо приложения, своя причина меняться. Отдельный контекст.

**Shared kernel** этих двух — `QuantityUnit` и `FormType`. Единица измерения обязана быть одной на систему; копия по контексту означала бы, что «шт» из каталога и «шт» из упаковки — разные вещи.

**Журнал приёмов** — отдельный контекст за портом. Причина не техническая: приёмы слишком персональны, чтобы лежать в нашей БД. Сегодня реализация в памяти процесса, завтра очередь или внешний сервис. Критерий, по которому проверяется, что граница проведена верно: **смена хранилища меняет репозиторий, а не сущность.**

## 2.2 Модель

### Упаковка — `Drug`, aggregate root

Физически это пачка. Её нельзя пополнить: две пачки — две упаковки. Пустая пачка выбрасывается.

Состояние: `id`, `medKitId`, `name`, `quantity: Quantity`, `formType?`, `category?`, `manufacturer?`, `country?`, `description?`, `version`.

Инварианты: количество строго положительно (нулевой упаковки не существует — она уничтожается); единица величины неизменна, кроме явной перевески.

Команды: `describe(details)`, `changeQuantityTo(q)` — исправление учёта, `consume(amount): Drug?` — `null` означает «пачка кончилась и подлежит уничтожению», `relabelUnitTo(unit)`, `moveTo(targetMedKitId)`.

**Чего в агрегате нет и не будет:** броней, их суммы, доступного остатка, пропорционального пересчёта. Упаковка не знает, кто на неё претендует.

### Бронь — `Reservation`, aggregate root

Заявка человека на часть упаковки. Идентичность — пара `(userId, drugId)`.

Состояние: `userId`, `drugId`, `amount: Quantity`. **Версии у брони нет** — токен предусловия принадлежит картине броней на упаковку (`ReservationSnapshot`): менять свою долю, не видя общей суммы, значит решать вслепую.

Инвариант ровно один: количество строго положительно. Отмена брони — это её удаление, а не бронь с нулём.

**Чего у брони нет:** ограничения сверху. Бронь может превышать остаток, и это не ошибка, а нормальное состояние, за которое отвечает владелец.

Связь с упаковкой — по идентификатору, не по ссылке. Бронь не переживает уничтожения упаковки: это правило целостности данных (внешний ключ с каскадом), а не отношение владения — JPA-каскада и `orphanRemoval` между ними нет.

### Аптечка — `MedKit`, aggregate root

Состояние: `id`, `members: Set<UUID>`, `version`. Инвариант: участник хотя бы один. Членство — дочерние строки `user_med_kits` внутри агрегата.

### Пользователь — `User`, aggregate root

`id` и `hashedKey`. Правило одно: пользователя без ключа не существует. Реализует `UserDetails` — принятое отклонение: домен зависит от `spring-security-core`, потому что пользователь и есть тот, кем его представляет аутентификация.

### Приём — `Intake`, entity

`id` (придуман клиентом), `userId`, `drugId`, `amount`. Идемпотентность — не приём кеширования, а следствие идентичности: приём с таким идентификатором либо записан, либо нет.

### Значения

`Quantity` — число и единица вместе; нормализация масштаба и вся арифметика внутри типа, величины в разных единицах не складываются. `QuantityUnit`, `FormType`, `DrugTemplate`.

## 2.3 Слои: где какая логика

| слой | что здесь живёт | чего здесь быть не должно |
|---|---|---|
| `domain` | **Бизнес-правила.** Инварианты в конструкторах; команды возвращают новое состояние, а не меняют текущее. Знание «упаковка не пополняется», «бронь может превышать остаток», «нулевой упаковки не бывает» — здесь и только здесь. | Аннотации персистентности, HTTP, знание о хранилищах. Из Spring — только `UserDetails`, отклонение названо выше. |
| `db/model` (`*Data`) | **Работа с ORM.** Отображение на строки и ничего больше. `var` — требование Hibernate, а не разрешение менять поля откуда угодно. | Любые решения. Ни одного метода с правилом. |
| `db/repository` | **Работа с БД.** JPQL, нативные запросы, массовый DML, индексы. Это адаптер Spring Data, а **не** Repository из DDD. | Доменные типы, транзакции, проверки доступа. |
| — | **`EntityManager` в рабочем коде запрещён совсем.** `flush`, `clear`, `detach`, `refresh`, `getReference` — оснастка тестов: ими подгоняют persistence context под ожидания. В проде они означают запись, выстроенную вокруг того, когда именно Hibernate что-то сделает. Держит `NoEntityManagerTest`. | |
| `db/store` (`*Store`) | **Repository в смысле DDD** — граница агрегата. Принимает и отдаёт только доменные типы; внутри — отображение, сборка агрегата, заявка на изменение корня. | Бизнес-правила, коды HTTP. |
| `services/aggregate` | **Один сервис на агрегат.** Прочитать состояние, вызвать метод домена, отдать хранилищу. Проверка доступа и правила, относящиеся к этому агрегату. Единственный, кто видит `db/store` своего агрегата. | Правила чужих агрегатов, SQL (за хранилищем), DTO, Spring MVC, **открытие транзакции**. |
| `services/orchestrator` | **Сложная комбинация агрегатов.** Домен на входе, домен на выходе. Появляется, когда сценарий нужен больше чем одному фасаду или несёт правило взаимодействия, а не последовательность. | **Что угодно про клиента:** DTO, коды ответа, клиентские идентификаторы. Открытие транзакции. |
| `services/application` | **Один сервис на ресурс** — то, что клиент и заказывает. Владеет транзакцией, переводит в DTO, зовёт несколько сервисов подряд и передаёт результат одного другому. Только это видит контроллер. | Сложная комбинация — она ниже. Хранилища. |
| `api` | DTO публичного контракта и сборка их из доменных типов. | Правила, обращения к БД. |
| `controller` | Перевод HTTP: разбор предусловий, коды, теги, валидация формы запроса. | Правила, репозитории, сущности. |

**Словарь, который надо держать в голове при чтении:** `db/repository` и `db/store` — разные вещи с обманчиво похожими именами. Первое — ORM-адаптер, второе — DDD-репозиторий. Переименование в свой PR; здесь фиксируется значение.

**Прикладной слой режется по сценарию, а не по числу задетых агрегатов.** Это ошибка, которую пришлось исправлять: деление «один агрегат / больше одного» — свойство реализации, и клиент про него ничего не знает. Оно протекло наружу — контроллер обязан был выбирать между сервисом и оркестратором по признаку, невидимому из HTTP, а класс, названный по агрегатам, которых касается, собрал в себе всё, что трогает два из трёх.

Правила, по которым слой держится:

1. **Контроллер видит один сервис на ресурс.** Сервис зовёт другие сервисы, но не хранилища: до агрегата ходят его сервисом.
2. **Фасад тонкий.** Ему позволено позвать несколько сервисов подряд и передать результат одного другому — и всё. Сложная комбинация уезжает в оркестратор или в сам агрегат.
3. **Фасады не зовут друг друга.** Понадобилось двоим — значит, общее лежит слишком высоко и должно уехать вниз, а не расти вбок.
4. **Оркестратор про клиента не знает.** Домен на входе и на выходе. Всё клиентское стекается в прикладной слой — и **поэтому** за его толщиной следят особо: дай знанию протечь ниже, и кандидат в боги просто сменит имя.
5. **«Можно ли делать операцию» решает тот, кто за неё отвечает.** Агрегат знает, кто он и как себя проверить; вызывающий спрашивает и отдаёт операцию дальше.
6. **Транзакцией владеет фасад.** Ниже — `propagation = MANDATORY`: границу требуют, но не открывают. Иначе владельца use case из кода не видно, и всё держится на `REQUIRED` по умолчанию.

Все шесть закреплены рефлексивным тестом.

Названное исключение одно: `AuthenticatedUserService` зовёт фильтр безопасности, и границы вокруг ещё нет — он открывает свою. Это точка входа наравне с фасадом, просто вход не из контроллера.

## 2.4 Доступ

> **Каждое чтение, отдающее данные наружу, принимает вызывающего.** Либо предикат членства стоит в самом запросе, либо принадлежность выражена ключом строки. Третьего варианта нет.

| агрегат | чем держится доступ |
|---|---|
| `Reservation` | **ключом**: составной внешний ключ ведёт на членство, поэтому брони без доступа не существует. Отдельного снятия броней при выходе больше нет — это тот же каскад |
| `Drug` | **предикатом**: `EXISTS` по `user_med_kits` через `med_kit_id` пачки |
| `MedKit` | **предикатом**: `EXISTS` по `user_med_kits` |

Отдельного шага «проверить доступ» нет: чтение и есть проверка. Поэтому и слова «accessible» в именах нет — различать нечего, другого чтения не бывает. Признак — сам параметр `userId` в сигнатуре, и его наличие проверяет `QueryScopeTest`.

Пессимистичных блокировок в чтениях нет: `@Lock(PESSIMISTIC_WRITE)` снят вместе с `lockAccessible`.

**Категории «родитель уже проверен» нет.** Она держалась на дисциплине вызывающего, а дисциплинарные правила забываются первыми.

**Объект — доказательство доступа, идентификатор — то, что надо проверить.** Отсюда разделение: **чтение принимает идентификатор** и несёт предикат, **команда принимает агрегат**. Если команде нужен идентификатор, она берёт его у объекта сама; принять его снаружи значит согласиться, что вызывающий проверку прошёл, — а проверил его никто. Читает фасад, один раз, и передаёт вниз уже доказанное. Держит правило `CommandsTakeAggregatesTest`.

**Перегрузка вместо чтения в фасаде.** Основная форма команды берёт агрегат; рядом стоит перегрузка по идентификатору, которая честно читает и зовёт основную. **Своё читает сервис агрегата, чужое — оркестратор:** сервису знать про соседний агрегат не положено. Фасад остаётся тонким — компоновка запроса и ответа, — и читает сам только там, где собирает ответ из нескольких агрегатов.

**Исключений нет, включая вступление по приглашению.** Оно перестало быть чтением чужой аптечки: `join` вставляет строку членства и читает аптечку уже как участник. Отсутствие аптечки и повторное вступление приезжают нарушениями именованных ограничений.

Разобранные альтернативы и почему не они:

- **Postgres RLS.** Обойти нельзя ничем, но правило уезжает в базу вопреки нашему же запрету прятать там логику, а личность вызывающего надо надёжно прокидывать в сессию через пул. Отклонено; к вопросу не возвращаться без новой причины.
- **Фильтры ORM (`@Filter`).** Не применяются к загрузке по идентификатору — то есть ровно к тому пути, на котором дыра и была.
- **`@PostAuthorize` / `@PostFilter`.** «Загрузить и выбросить»: грузит недоступное в память и ломает пагинацию.
- **ReBAC-сервис (Zanzibar, SpiceDB, OpenFGA).** Несоразмерно трём агрегатам и требует держать отдельную систему согласованной с собственной базой.

## 2.5 Конкурентность и целостность

Три разных вопроса, которые легко свалить в один механизм — и один раз уже свалили.

| вопрос | чем отвечаем |
|---|---|
| «есть ли у человека право на это?» | **запросом**: читаем то, что видно вызывающему, и не находим чужого |
| «согласованы ли связанные строки?» | **ключами**: правило живёт в коде, ключ страхует |
| «не затёр ли я чужую запись?» | **версией** той сущности, которую записываю |

**Версия отвечает только за свою сущность.** Она не артефакт доступа: нужно проверить, что человек всё ещё в аптечке, — идём и проверяем, а не удерживаем чужой токен. Прошлый заход держал версию упаковки и состав аптечки «как основание решения», и это разъезжалось: версия аптечки двигалась от вступления постороннего, а отказ получал тот, кого это не касалось.

| механизм | что делает | где |
|---|---|---|
| предикат версии в `UPDATE` | ловит потерянное обновление: ноль задетых строк — гонку проиграли | упаковка, снимок броней, аптечка |
| отдельный `UPDATE` по строке корня | двигает версию, когда меняется агрегат, а не его строка | только аптечка: состав лежит в другой таблице |

**Команда предъявляет версию того, к чему обращается.** Упаковка — версию упаковки, бронь — версию брони, аптечка — версию аптечки (выход, удаление, удаление с переносом). Создание упаковки, приглашение и вступление предусловия не требуют: они ничего не перезаписывают.

Версия двигается тем же оператором, что и состояние: `version = предъявленная + 1` идёт частью `UPDATE`, а предикат `version = предъявленная` стоит в его условии — от прочитанного объекта здесь не берётся ничего, иначе версия могла бы уйти вниз. Присваивать её отдельно нечему — Hibernate, который ею распоряжался, ушёл в #96.

**Замерено:** заявка на изменение корня применяется перед коммитом, а не при `flush`, и складывается с обычным приращением от dirty checking. И: массовый DML с `clearAutomatically` очищает persistence context, унося вместе с ним зарегистрированную `OPTIMISTIC`-проверку — проверено вычитанием, снял из сценария один этот запрос, и та же гонка стала отвергаться. Второй замер — причина, по которой удержание чужого агрегата не выжило бы даже технически.

Несовпавшее предусловие — 412: оно предъявлено и не выполнено. Проигранная гонка отвечает так же — предъявленное было верным на чтении и устарело к записи, но база различает эти случаи одинаково, нулём задетых строк, да и клиенту разницы нет. 409 остаётся конфликтам, предусловием запроса не бывшим: дублю брони и версиям из тела синхронизации. Авто-повтора нет ни там, ни там: сервер не знает, останется ли команда осмысленной по новому состоянию.

### Правила в коде, ключи — сеть безопасности

Бронь живёт с упаковкой и человеком, а не с членством, и переезжает вместе с пачкой. Правило одно:

> **Назначение переживает переезд, если человек допущен к целевому хранилищу, и снимается, если не допущен.**

Коробку переставили на другую полку; для того, кто к полке допущен, ничего не изменилось. Правило описано в `DrugRelocation` и в домене (`Reservation.survivesRelocationTo`) — читающий DDD-слой обязан увидеть его целиком, не заглядывая в схему.

Схема то же самое **страхует**: `med_kit_id` на броне и составные внешние ключи `(drug_id, med_kit_id) → user_drugs(id, med_kit_id)` с `ON UPDATE CASCADE` и `(med_kit_id, user_id) → user_med_kits` с `ON DELETE CASCADE`. Гонка между переездом и выходом не оставит брони без доступа даже там, где код не успел.

**Триггеров в базе нет.** Hibernate не видит изменений, сделанных мимо него: строка, обновлённая триггером, разъезжается с версией в памяти, и ни `@Generated`, ни `@Formula`, ни колбэк JPA этого не закрывают — замерено. Логика — в бизнес-правилах, база держит ключи.

## 2.6 Синхронизация

Клиент копит события офлайн и присылает суммы, а не поток. Но две кумулятивные величины — сколько съедено и какой стала бронь — это два разных ресурса, и порядок между двумя запросами не гарантирован. Разрыв реален: приходит списание, срабатывают уведомления «лекарства мало», и только потом уменьшение брони.

Поэтому единый запрос, атомарный **на упаковку**:

```
PUT /v1/drugs/{drugId}/sync/{syncId}
{
  "consumed": "5.0",          // дельта; необязательно
  "drugVersion": 3,           // предусловие упаковки
  "reservation": {            // необязательно
    "amount": "20.0",         // абсолютное значение
    "version": 7              // предусловие брони; отсутствует, если брони ещё нет
  }
}
```

Версии переезжают из заголовка в тело **только здесь**: запрос меняет два ресурса, а заголовок один. В остальных местах предусловие остаётся заголовком. Отсюда и код ответа: несовпадение версии из тела — 409, а не 412.

Журнал повторов пишется **после коммита**. Кеш не участвует в транзакции, и запись по ходу команды пережила бы откат — повтор получил бы подтверждение того, чего в базе нет, а клиент вычистил бы офлайн-очередь.

## 2.7 Маршруты после изменений

| маршрут | что делает |
|---|---|
| `POST /v1/drugs/{drugId}/intakes` | единичный приём: списывает из упаковки. Заменяет `consumptions` — различия «по плану» и «вне плана» больше нет |
| `PUT /v1/drugs/{drugId}/sync/{syncId}` | пакетно и атомарно: приём вместе с новой бронью |
| `GET /v1/reservations`, `GET/PATCH/DELETE /v1/reservations/{drugId}` | брони вызывающего; переезд с `/v1/treatment-plans` |
| `PUT /v1/intakes/{intakeId}` | уходит в 404: маршрут отвечал `501` и ни разу не работал |

### Формы ответов

`DrugDTO` теряет `availableQuantity` — такого понятия больше нет. Имена словарей (`quantityUnit`, `formType`, `category`) в нём тоже не нужны: единственное, что даёт справочная запись, — название, и его отдаёт словарь.

Вместо голого `DrugDTO` маршруты упаковки отдают **снимок**:

```
DrugSnapshotDTO { drug, reservations { total, mine } }
```

Пачка не знает про брони, но клиенту нужны обе величины сразу — сумма заявленного и своя доля. Собирается это в `api`, чтением броней одним запросом на весь набор.

Аптечка отдаёт **состав**, а не содержимое: список идентификаторов упаковок, а не вложенные объекты. Снимок пользователя — `{ id, medKits, drugs }`: плоско, без дублирования одной пачки в двух местах.

---
---

# Часть 3. План работ

Ветка прошлой попытки (`codex/jpa-ddd-08-optimistic-versioning`) удаляется, все новые ветки создаются от `main`. Каждый PR уходит прямо в `main`; внутри PR — перечисленные ниже коммиты, каждый компилируется и оставляет тесты зелёными.

## PR-A `codex/ddd-package-and-reservation` — упаковка и бронь ✅ слит (#88)

До перестройки доставки; дальше нумерация сквозная.

Смена концепции. Версий и синхронизации здесь нет.

**Коммит 1. Бронь отделяется от упаковки.**
Модель, схема, хранилища и сервисы переезжают одним куском — иначе не компилируется, и это честная единица.
- `domain/Drug.kt`: убрать коллекцию броней, `plannedTotal`, `availableQuantity`, `plansScaledTo`, `applyIntake`, команды планов. Оставить `describe`, `changeQuantityTo`, `consume`, `relabelUnitTo`, `moveTo`.
- `domain/Reservation.kt`: новый корень агрегата, инвариант «количество положительно».
- `domain/DomainErrors.kt`: убрать `PlannedAmountExceedsStock`, `IntakeExceedsPlan`, `ForeignTreatmentPlan`; переименовать `NoSuchTreatmentPlan` и `TreatmentPlanAlreadyExists` под бронь.
- `db/schema.sql`: `usings` → `reservations`; внешний ключ на упаковку сохраняет `ON DELETE CASCADE`, на пользователя — без каскада, как и раньше.
- `db/model`: `ReservationData` — самостоятельная сущность; из `DrugData` уходит `@OneToMany` с каскадом и `orphanRemoval`.
- `db/store`: `ReservationStore`; `DrugStore` теряет всё про планы; читающие запросы упаковок больше не делают `join fetch` броней.
- `services/models`: `ReservationService` — чтения и команды брони; `DrugService` остаётся при упаковке.
- Контроллеры переводятся на `ReservationService`, **маршруты пока прежние** — так коммит остаётся зелёным без правки контракта.
- Тесты: `DrugAggregateTest` теряет проверки пересчёта и суммы; новый `ReservationTest`; интеграционные — «бронь больше остатка это нормально», «уничтожение упаковки уносит бронь», «списать больше упаковки нельзя»; `PlanReconciliationTest` удаляется как проверка исчезнувшего правила.

**Коммит 2. Контракт: брони вместо планов лечения.**
- `/v1/reservations` и `/v1/reservations/{drugId}`; `/v1/treatment-plans` отдаёт 404.
- `POST /v1/drugs/{drugId}/intakes` вместо `consumptions`; `PUT /v1/intakes/{intakeId}` отдаёт 404.
- `DrugDTO`: `availableQuantity` уходит; `plannedQuantity` собирается чтением броней одним запросом.
- Тексты новых операций в `OPERATION_TEXTS` — без них сборка падает.
- Контрактный тест маршрутов и снимок пользователя обновляются; `open-api.yaml` перегенерируется, diff вычитывается глазами.

**Коммит 4. Композиция уезжает в сервисы, комментарии чистятся.**
- `MedKitDrugOrchestrator.drug(drugId, userId)` и `drugsAccessibleTo(userId)`: сборка `DrugDTO` из упаковки и её броней — одна, в оркестраторе. Из `DrugController` уходит приватный `view(...)`, из `UserController` — зависимость от `ReservationService`, которая ему не нужна.
- `ReservationService.onDrug` удаляется: остаётся `onDrugs`, читающий весь набор одним запросом.
- Сплошной проход по комментариям: исторические записи (в том числе про объекты, которых больше нет) убраны, слишком большие сжаты. Затронуты домен, `db/*`, `services/*`, `api/*`, контроллеры и тесты.
- `integration/GlobalIntegration.kt` удалён: пустой класс из одних заголовков про пропорциональный пересчёт.
- Долг, записанный, а не исправленный здесь: имена тестов и файл `TreatmentPlanStoriesTest.kt` всё ещё говорят «treatment plan»; переименование — отдельным коммитом, чтобы не мешать diff комментариев.

## Перестройка доставки: #89, #90, #91 закрыты

Три PR читать было нельзя. Не из-за размера: #89 вводил подходы, которые #90 и #91 отменяли, и рецензент не мог отличить настоящую проблему от уже исправленной дальше по стеку. Внутри стека рождались и умирали `DrugSyncOrchestrator`, `IntakeJournalStore` с портом в домене и `MedKitStore.requireUnchanged` с пятью удержаниями доступа; три фичи уезжали недоделанными и чинились через PR.

Работа собрана заново от `main` шестью PR. Ни один не вводит того, что следующий отменяет; поздние дополняют ранние, как обычная фича.

## PR 1 — схема под присмотром приложения и числовой контракт ✅ слит (#92)

`db/schema.sql` создаёт базу в тестовом контейнере, приложение поднимается с `validate`; файл схемы и `open-api.yaml` объявлены входами задачи `test` — без этого правка схемы не инвалидировала прогон. `@Digits(13, 6)` на всех входящих количествах, граница в опубликованной схеме, `CHECK (quantity > 0)` и `CHECK (amount > 0)`.

Идёт первым: дальше схему правят три PR, и все — уже под присмотром.

## PR 2 — прикладной слой по сценариям ✅ слит (#93)

Исправление просчёта части 2.3: слой был разрезан по числу задетых агрегатов, и это протекло в контроллеры.

`services/models` → `services/aggregate`, `MedKitDrugOrchestrator` удалён. По одному фасаду на ресурс в `services/application`, оркестратор `DrugRelocation`, фасады справочника и аутентификации. Контроллер видит один сервис. Транзакцией владеет фасад, ниже — `MANDATORY`. Внутренние команды `NewDrug`/`DrugEdit`: форму запроса переводит фасад. Сборка ответа — в `api/Mappers.kt`. Границы закреплены `LayerBoundariesTest` по импортам исходников.

Удержаний доступа здесь нет и не появится ни в одном следующем PR.

## PR 3 — доступ в модели и запросах ✅ слит (#94)

Порядок был выстроен неверно: сервисы написали раньше, чем модель, на которой они стоят, и #93 слит с ручной заплаткой вместо механизма. Этот PR ставит механизм и снимает заплатку.

**Схема.** `UNIQUE (id, med_kit_id)` на `user_drugs`; `med_kit_id` у брони; два составных ключа вместо двух одиночных — `(drug_id, med_kit_id) → user_drugs ON UPDATE CASCADE ON DELETE CASCADE` и `(med_kit_id, user_id) → user_med_kits ON DELETE CASCADE`; индекс `(med_kit_id, user_id)`. Бронь без членства становится невыразимой, а не убираемой.

**Запросы.** Чтение аптечки по идентификатору с `EXISTS`; `findAllInMedKit` и `findAllOfDrugs` принимают вызывающего; `join` вставляет членство и читает уже скоуплено; нарушения именованных ключей переводятся в доменные отказы. Закрепляется тестом: публичного чтения хранилища без вызывающего не остаётся.

**Починка #93.** Фасад удаления аптечки читал её дважды и выбрасывал первый результат; `requireAccessible` был «загрузить и спросить домен». Фасад читает один раз и передаёт агрегат вниз, `MedKit.requireMember` уходит.

## PR 4 — контракт: представление отвечает само за себя ✅ слит (#95)

`DrugDTO` теряет имена словарей и `reservedQuantity`. Появляется `DrugSnapshotDTO { drug, reservations { total, mine } }` — и на выдачу, и на приём. Аптечка отдаёт состав, а не содержимое. Снимок пользователя — `{ id, medKits, drugs }`. `DrugTemplateDTO` тоже без имён.

Идёт **до** версий, чтобы версия сразу легла в окончательные формы.

## Экспериментальный переезд, ставший этапом: #96 и #97 — на ревью

Не было в плане и появилось из наблюдения: весь #94 состоял из расплат за persistence context, ни одна из которых не про предметную область. Замер показал, что ORM мы платим полную цену, не получая ничего, — и переезд назначен **до версий**, потому что иначе `@Version` и предусловия строились бы на Hibernate и выбрасывались бы следом.

**#96 — Exposed вместо Hibernate.** `db/model` и `db/repository` исчезают, запросы переезжают внутрь хранилищ, публичная поверхность `db/store` не меняется. Переезд нашёл настоящую ошибку: `DrugRelocation.moveOne` менял порядок операторов, и Hibernate скрывал это избирательным сбросом.

**#97 — провод и идентификатор говорят на Kotlin.** `kotlin.uuid.Uuid` от колонки до JSON, kotlinx.serialization на DTO, величины едут строкой. Kotlin 2.4, Java 25. `open-api.yaml` не сдвинулся ни на байт.

Часть 2.3 теряет абзац-словарь «`db/repository` против `db/store`»: второго слоя больше нет.

## PR 5 — схема из одного источника ✅ слит (#99)

**Дыра, которую открыл #96, и её надо закрыть раньше, чем схему начнут править версии.**

`SchemaValidatesTest` держал согласие `db/schema.sql` с моделью суждением самого Hibernate: база создавалась файлом, приложение поднималось с `validate`. Hibernate ушёл, тест удалён — и теперь прод поднимается по `db/schema.sql`, а тесты по `Tables.kt`, и разойтись они могут молча. Ровно те два источника истины, против которых тест и стоял.

Вместе с ним ушли ещё два, и оба зря:

- `CascadeSchemaTest` проверял каскады в самой базе — правило части 1 «без переноса содержимое уходит каскадом». К ORM отношения не имел, удалён заодно.
- `SchemaContractTest` проверял свойства самого файла: создание с нуля без `ALTER`, точный тип количеств, каскады у детей и их отсутствие у пользователя, `CHECK` на положительность. Устарел в нём ровно один пункт из пяти — про `ddl-auto=validate`.

**Решение — один источник.** `Tables.kt` описывает схему, `db/schema.sql` из него порождается, и тест сверяет порождённое с закоммиченным, как `OpenApiSnapshotTest` сверяет контракт. Тогда прод и тесты поднимают одно и то же по построению, а не по договорённости.

Возвращаются `CascadeSchemaTest` целиком и `SchemaContractTest` без пункта про `validate`.

## PR 6 — версии, предусловия и синхронизация ✅ слит (#100)

`version` в схеме и домене; заявка на изменение корня у аптечки; массовый DML двигает версию сам. Коды 428/400/412/404. Команды возвращают записанную версию.

**По ревью переделано главное.** Предъявленная версия стала обязательным параметром записи: `save(drug, stated)` не собирается без неё, и предикат `UPDATE` берёт предъявленное, а не версию прочитанного. `Preconditions` с забываемым `requireStated` исчезли; в теле версию разворачивает превращение запроса в команду, ниже границы контракта обнуляемых версий нет. Отсюда и код: проигранная гонка отвечает **412**, как и всякое несовпадение, — различать «прислал устаревшую» и «проиграл гонку» база не даёт. 409 остался дублю брони и версиям из тела синхронизации.

**Переезд сделал этот PR проще, и риск из него ушёл.** `@Version` у Exposed нет, и не нужен: предикат версии пишется в самом `UPDATE`, а проигранная гонка видна по числу задетых строк. Замеренная несовместимость `@Version` с триггерами перестала существовать вместе с `@Version` — сравнивать и увеличивать версию теперь нечему, кроме нашего же запроса.

Тесты гонок: списание, членство, перенос против выхода, одновременное создание. Проигравшая сторона обязана получить отказ, а состояние — совпасть с результатом победившей.

**Сюда же перевод нарушений именованных ключей в доменные отказы.** Правила «одна бронь на пару» и «дважды не вступают» выражены в коде и проверяются чтением; ключи их страхуют, но на настоящей гонке отдают нарушение ограничения. Переводить его в отказ имеет смысл там, где гонки заводятся под тест. Оговорка про другие имена ограничений на H2 снимается: H2 из сборки ушёл, тесты идут на Postgres.

## PR 7 — синхронизация пачки ✅ вошла в #100

`Intake`, `IntakeStore`, `IntakeService` — сразу хранилищем, без порта в домене. Оркестратор `DrugSynchronisation`, маршрут `PUT /v1/drugs/{drugId}/sync/{syncId}`. Журнал пишется в `afterCommit` с самого начала. `StaleSyncVersion` → 409. Ответ — тот же `DrugSnapshotDTO`, что у остальных маршрутов пачки.

## PR 8 `codex/queryplan-suite` — планы запросов ✅ открыт (#101)

Отдельный source set `queryPlanTest` после `test`; `RecordingDataSource` через BeanPostProcessor; большой фикстур (200 пользователей, 300 аптечек, 10 000 упаковок, ~30 000 броней, ~18 000 записей справочника); `EXPLAIN (FORMAT JSON)` в естественном и forced-index режимах с восстановлением `enable_seqscan` в обоих исходах; проверка фактических индексов и отсутствия недопустимых Seq Scan; version-предикаты в optimistic UPDATE; реестр публичных методов хранилищ, чтобы ни один не остался без измерения; отчёты артефактом CI, в репозиторий не коммитятся.

Реестр стал проще: методов теперь один слой, а не два.

## PR 9 `codex/architecture-rules` — правила части 2 рефлексивно ✅ открыт (#102)

Набор ужимается: половина правил стала невыразимой, потому что нечего запрещать. Ушли «контроллер не импортирует репозитории и сущности» и запрет `EntityManager` — ни репозиториев, ни сущностей, ни `EntityManager` больше нет.

Остаются: контроллер не импортирует больше одного сервиса; прикладной сервис не импортирует хранилища; сервис не импортирует контроллеры, DTO и Spring MVC; домен не зависит от API и не бросает HTTP-исключений; читающие методы не мутируют; **в проде нет `JdbcTemplate`, `FOR UPDATE` и пессимистичных локов** — правило стало и выполнимым, и проверяемым: нечёткий поиск больше не спускается к драйверу, и единственный `JdbcTemplate` остался в тесте, который нарочно ходит мимо приложения; на понятие не больше одного типа в домене и одного в отображении; нет коллекций между агрегатами; нет transactional self-invocation.

## PR 10 `codex/nplusone-gates` — N+1 ✅ открыт (#107)

**Переписан целиком: механизм замера был Hibernate Statistics, которого нет.** Число запросов снимает `RecordingDataSource` из PR 8 — один инструмент на оба PR вместо двух, и он же честнее: считает то, что реально ушло в базу, а не то, что насчитала ORM.

Сценарии масштабирования (снимок 0/1/5/25 аптечек, брони 0/1/250, содержимое аптечки 0/10/100 упаковок, выход и удаление 1/100); Θ(1) для чтений, массовых операций и каскадов; запрет «успешного» сценария с нулём SQL там, где обращение обязано быть; отчёт артефактом.

**Пустота меряется отдельным правилом.** Оказалось, что пустой снимок делает 2 запроса вместо 4: без упаковок читать брони не по чему. Это короткое замыкание, а не нарушение, поэтому от пустоты требуется не равенство, а «не дороже наполненного».

**Заодно выяснилось, что #101 не доехал до main:** слит в `codex/versions-and-sync` уже после того, как та ветка ушла в main через #100. Возвращён отдельным PR (#106) — и при слиянии оказалось, что набор планов не компилируется: писался до того, как предъявленная версия стала обязательным параметром записи.

## PR 11 `codex/contract-and-docs` — контракт и документация ✅ открыт (#108)

Единственный осмысленный проход по описаниям и примерам; семантика версий и предусловий; сверка с архивными тегами прошлых итераций, чтобы ни одна функция не потерялась; `ARCHITECTURE.md` из частей 1 и 2 этого документа.

Сюда же — пример величины: springdoc теряет хвостовые нули, разбирая `example` до того, как его видит кастомайзер, и в контракте стоит `"100.0"` там, где сервер отдаёт `100.000000`.

**Замерено:** `examples` (множественное число, форма 3.1) springdoc оставляет текстом. Величины переведены на него, а `StringExampleCustomizer` вместе с этим стал мёртвым и удалён — проверено вычитанием.

**Сверка с архивными тегами: потерь нет.** Всё переехало под `/v1`; единственный не переехавший — `GET /drug/quantity/{id}`, свёрнут в чтение упаковки.

**Найдено по дороге:** документация не устарела, а врала — README описывал маршруты, отдающие 404 с #95. Список маршрутов теперь сверяется с контрактом тестом, а `README.md` объявлен входом тестовой задачи: без этого гейт отвечал бы «up-to-date» ровно в том случае, ради которого заведён.

**Оснастка переведена на kotlinx:** контракт и вывод `EXPLAIN` разбирались Jackson, хотя на проводе kotlinx. Jackson остался в одном законном месте — `ProblemDetail` без `@Serializable`.

## PR 12 `codex/supply-chain` — цепочка поставок ✅ открыт (#109)

Три задуманных PR сведены в один по просьбе: сканеры, обновления и права workflow ставятся разом. CodeQL идёт в режиме `build-mode: none` — анализатору не нужен ни JDK 25, ни Android SDK; область ограничена сервером файлом настройки, иначе в `java-kotlin` сам попадает Android.

Скраппер вынесен в отдельный публичный репозиторий `MedApp-scrapper` и подключён подмодулем: одноразовый код держал в основном репозитории вторую экосистему в области сборки и сканирования. Репозиторий заархивирован.

`verification-metadata.xml` отложен в долги.

## PR 13 — починка найденного ✅ открыт (#111)

Написан по первому отчёту, а не заранее. Обе находки CodeQL (`java/spring-disabled-csrf-protection`) закрыты как ложные — обоснование не «сейчас кук нет», а правило: `StatelessAuthTest` требует `STATELESS` от каждой цепочки отдельно и запрещает сессионное API. Снять правило — значит открыть находки заново, и это записано в `ARCHITECTURE-RULES.md`.

Главное найденное — само собой: гейт `dependency-review` сторожил пустой список. В графе стояли восемь записей, все восемь — сами actions: у Gradle зависимости известны только после разрешения конфигураций, и дерево надо подавать. Добавлена подача `gradle/actions/dependency-submission` на push в `main`. Заодно включены оповещения об уязвимостях и автоматические security-PR — это настройки репозитория, поэтому названы в README.

## PR 14 — разбор первого непустого отчёта ✅ открыт (#115)

Подача графа дала 74 оповещения. Ни одного прямого объявления: уязвимы транзитивные, которыми управляет BOM самого Boot, — лечит не список исключений, а один пин `4.0.2 → 4.0.8`. Закрывает 69 из 74, сверено резолвом по всем конфигурациям, а не на глаз.

Оставшиеся пять — не в приложении: `httpclient5`, `httpcore5`, `httpcore5-h2` и `commons-lang3` приезжают в `spring-boot-buildpack-platform`, то есть в сам Gradle-плагин Boot; у `kotlin-gradle-plugin` исправленной названа бета.

## PR 15 — комментарии объясняют настоящее ✅ открыт (#113)

Последовательный проход по всем 138 файлам исходников. Уходит история правок, пересказ соседней строки и риторика; остаются правило, причина выбора, замеренное поведение чужого кода и предупреждение о ловушке. Оснастка переведена на один язык.

Найдено по дороге: три KDoc врали, комментарии объясняли поведение через Hibernate, которого нет, а заголовки секций и имена тестов звали методы, которых нет. Долг «имена тестов про treatment plan» оплачен здесь же — по своему поводу.

## PR 16 — контракт объявляет обязательные поля ✅ открыт (#114)

Закрывает #112. Обязательность и обнуляемость публикуются из `SerialDescriptor` — из того же описания, по которому пишутся тела. Причина молчания springdoc: обязательность swagger-core выводит через Jackson, а `jackson-module-kotlin` ушёл вместе с переходом на kotlinx.

## PR 17 — README как страница репозитория ✅ открыт (#116)

Сервер живёт отдельным репозиторием, а README начинался со списка технологий. Добавлено вступление, три решения, вокруг которых построена модель, и таблица «с чего начать».

---

## Долги

Записаны, а не сделаны; каждый — с поводом, по которому за него возьмутся.

- **Таблица `drug_sync_receipts` вместо кеша.** Журнал в памяти не переживает рестарт и не работает между экземплярами — это названо в описании операции. Транзакционная таблица (HMAC ключа и намерения, признак «пачка уничтожена», retention) закрывает и это, и повтор уничтожившего пачку запроса — 204 вместо 404. Повод: второй экземпляр приложения.
- **Пагинация снимков.** `GET /v1/users/me` и содержимое аптечки собираются целиком в памяти. Число запросов постоянное (меряет PR 9), объём — нет. Повод: первая аптечка, где это станет заметно.
- **Kotlin 2.4.20, когда выйдет релиз.** CVE-2026-53914: десериализация метаданных build cache даёт исполнение кода. Оповещение закрыто как терпимое — кэш сборки выключен (ни `org.gradle.caching`, ни блока `buildCache`; `setup-gradle` кэширует Gradle user home, а не build cache), а исправленных релизов нет, только `2.4.20-Beta1/Beta2/RC`. Повод: выход 2.4.20 — или первая попытка включить кэш сборки, что вернёт находку раньше.
- **`gradle/verification-metadata.xml`.** Контрольные суммы всех зависимостей: тысячи строк, которые ломаются от каждого обновления, то есть от каждого PR dependabot. Повод: когда обновления пойдут ровно и станет видно, сколько их в неделю.
- ~~**Имена тестов про «treatment plan».**~~ Оплачено в #113: повод наступил — тот PR трогал все эти файлы. `TreatmentPlanStoriesTest` стал `ReservationStoriesTest`, имена и сообщения проверок названы бронями. Осталось одно упоминание — список `RETIRED` в `ApiTestClient`, где пути обязаны совпадать посимвольно.
- **Пустышка `flushAndClear` и её 98 вызовов.** Метод не делает ничего: синхронизировать нечего, запрос в Exposed всегда идёт в базу. KDoc об этом говорит прямо, но вызовы стоят в тестах и читаются как «здесь важна синхронизация». Повод: отдельная механическая правка, ничего не ждёт.

---

## Регламент

1. Каждый PR — прямо в `main`, с зелёными тестами и полным описанием. PR не сливаю сам.
2. **Сначала открываю PR с описанием, потом работаю.** Читать нечего, пока PR нет: ветка → первый шаг → PR (черновиком, если не закончено) с полным замыслом → остальная работа, дополняя описание по ходу.
3. Индексирую **только файлы своего PR** явными путями (`src/MedAppServer/src`, `src/MedAppServer/db`, `src/MedAppServer/open-api.yaml`): рядом лежат untracked-файлы пользователя, `git add -A` запрещён.
4. **Модель и запросы идут раньше кода, который на них стоит.** Сервисы, написанные под чтения, которые вот-вот изменятся, приходится переписывать — ровно тот случай, которого требует избегать пункт про недоделанные фичи. Проверено на #93: прикладной слой слит с ручной проверкой доступа вместо механизма, и следующий PR её снимает.
5. Каждый PR и каждый коммит делает одну вещь. Замеченный по дороге беспорядок записывается сюда, а не чинится на месте.
6. Ни один эндпойнт не публикуется в полуработающем виде.
7. Число запросов и производные величины проверяются **по SQL-логу, а не рассуждением**.
8. Механизмы фреймворка проверяются замером, а не документацией по памяти; результат замера идёт в описание PR.
9. `open-api.yaml` регенерируется механически по ходу; осмысленный проход — один раз, в PR 10.
10. На каждый PR — **свой подплан** отдельным файлом с подробным разбором. Этот документ обновляется при планировании, а не дописками задним числом.

## Что осталось

Сервер закончен: #113, #114, #115, #116 слиты, рабочие ветки удалены — локальные и удалённые, — а зонтик закреплён на `d2e289e`. Сохранены только те, что что-то хранят: `server-dev`, `app-dev`, `rescued/stash-0`, `rescued/stash-1` и тринадцать архивных тегов.

Открыто одно: **MedAppAndroid#1**. Пока оно не слито, указатель зонтика на клиента стоит на его `main`, а рабочая копия — на ветке PR; отсюда `M src/AndroidApp` в статусе зонтика. После слияния `git submodule update --remote src/AndroidApp` и коммит закрывают и это.

## Критерий завершения

Упаковка и бронь разделены, и каждая отвечает за себя; сервер не принимает решений за владельца брони; синхронизация клиента не оставляет разрыва между списанием и бронью; конкурентность держится только на версиях, `FOR UPDATE` и пессимистичных локов в проде нет; на понятие приходится по одному типу в домене и в отображении; схема живёт в одном источнике — прод и тесты поднимают одно и то же по построению; границы закреплены архитектурными тестами, N+1 — счётчиком запросов, планы запросов — `EXPLAIN`; все фичи части 1 отмечены ✅.
